image: maven:3-openjdk-17

stages:
  - build
#  - test
 # - deploy

cache:
  paths:
    - .m2/repository
  key: "$CI_JOB_NAME" # Identificador único para evitar conflictos.

build:
    stage: build
    script:
        - echo "Build"
        - 'mvn clean install --settings ci_settings.xml'